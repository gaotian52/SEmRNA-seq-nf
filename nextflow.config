manifest {
    mainScript = 'main.nf'
}

params {
  output = "results/"
}

process {
    container = 'faithman/semrna-seq-nf'
    publishDir = [path: params.output, mode: 'copy', overwrite: 'true']
}

